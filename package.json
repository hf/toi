{
  "private": true,
  "workspaces": [
    "packages/toi",
    "packages/toix",
    "packages/dynamo"
  ],
  "scripts": {
    "build": "wsrun build --stages",
    "prettier": "prettier -l **/*.{ts,json,md}",
    "coverage": "wsrun coverage",
    "test": "wsrun test --serial",
    "publish": "wsrun publish"
  },
  "devDependencies": {
    "wsrun": "^5.0.0"
  },
  "dependencies": {
    "travis": "^0.1.1"
  }
}
